= form_for @loc, url: reverse_path, method: :post do |f|
  .row
    = f.label :locator, t(".enter_locator")
    = f.text_field :locator, required: true
    %p.format_info= t(".format_info")
  .row
    = f.submit t(:submit)
= link_to t(:home), root_path, class: :index_link